/**
 * SPDX-FileCopyrightText: 2026 Néfix Estrada
 * SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * The unified Nextcloud stylelint config, over the styles that are written
 * by hand: the Vue components' <style> blocks. css/ is build output —
 * linting what the compiler writes would gate nothing.
 */
export default {
	extends: ['@nextcloud/stylelint-config'],
}
